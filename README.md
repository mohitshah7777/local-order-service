# Codingly.io: Base Serverless Framework Template

## Getting started
```
cd YOUR_PROJECT_NAME
npm install

for serverless offline
install sls-offline
run sls offline

You are ready to go!
# local-order-service
